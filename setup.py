from setuptools import setup, find_packages

setup(
    name="occupancy_grid_rl",
    version="0.1.0",
    packages=find_packages(),
    description="RL training for occupancy grid navigation",
    author="",
    install_requires=[],
    package_dir={"": "."},
)
